# Minimal runtime deps for demo, API, and Hugging Face Docker (fast install).
# openenv-core pulls openai/fastmcp etc. and can stall Spaces builds — optional: pip install ".[openenv]"
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3
gradio>=4.44.1
requests>=2.31.0
huggingface_hub>=0.24.7
